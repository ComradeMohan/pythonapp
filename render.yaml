services:
  - type: web
    name: course-enroller
    env: python
    buildCommand: ""
    startCommand: gunicorn app:app
    envVars:
      - key: TWILIO_ACCOUNT_SID
        fromDashboard: true
      - key: TWILIO_AUTH_TOKEN
        fromDashboard: true
